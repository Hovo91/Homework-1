function isNumDigitsOdd(number) {
    let lastDigit = number % 10;
    if (lastDigit % 2 !== 0 && number !== 0) {
        return isNumDigitsOdd(Math.floor(number / 10))
    } else if (number !== 0) {
        return false;
    }
    return true;
}

console.log(isNumDigitsOdd(7791));
